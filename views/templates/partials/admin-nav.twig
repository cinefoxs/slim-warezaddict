<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
  <div class="container">
    <a class="navbar-brand" href="{{ path_for('index') }}">WarezAddict Admin</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle Navigation">
      <span class="navbar-toggler-icon"></span>
      <!-- <span class="fas fa-2x fa-theater-masks"></span> -->
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="moviesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Users</a>
          <div class="dropdown-menu" aria-labelledby="moviesDropdown">
            <a class="dropdown-item" href="{{ path_for('index') }}">List Users</a>
            <a class="dropdown-item" href="#">Link Two</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#">Link Three</a>
            <a class="dropdown-item" href="#">Link Four</a>
            <a class="dropdown-item" href="#">Link Five</a>
          </div>
        </li>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item gravatar-nav">
          {{ auth.user.gravatar(auth.user.email, 32) | raw }}
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">{{auth.user.name}}</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('auth.password.change') }}">Settings</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('auth.signout') }}">Sign Out</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
